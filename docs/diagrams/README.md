# Diagrams

This directory contains editable diagram sources.

Preferred formats:

- Mermaid
- PlantUML
- Draw.io
- SVG

Exported images should only be committed when required for Markdown rendering.

Use descriptive file names, for example:

```text
system-context.mmd
logical-architecture.puml
deployment-flow.drawio
```
